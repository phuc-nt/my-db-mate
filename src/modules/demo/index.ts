/**
 * Demo dataset: public surface.
 *
 * One export — the setup flow and the onboarding path both just need the demo
 * connection to exist.
 */
export { ensureDemoConnection } from './demo-service';
